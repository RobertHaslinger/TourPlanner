<Window x:Class="TourPlanner.Views.CreateTourView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TourPlanner.Views"
        xmlns:factory="clr-namespace:TourPlanner.Helper.Factory"
        xmlns:factory1="clr-namespace:TourPlanner.ViewModels.Factory"
        mc:Ignorable="d"
        Title="Create a new Tour" Height="450" Width="800"
        factory:ViewModelLoader.FactoryType="{x:Type factory1:CreateTourViewModelFactory}">
    <StackPanel
        Margin="0 5">
        <!--Starting Location-->
        <StackPanel
            Margin="5"
            Orientation="Horizontal">
            <Label
                MinWidth="50">Start:</Label>
            <TextBox
                MinWidth="200"
                Text="{Binding StartLocation, Mode=TwoWay}"></TextBox>
        </StackPanel>

        <!--End Location-->
        <StackPanel
            Margin="5"
            Orientation="Horizontal">
            <Label
                MinWidth="50">End:</Label>
            <TextBox
                MinWidth="200"
                Text="{Binding EndLocation, Mode=TwoWay}"></TextBox>
        </StackPanel>
    </StackPanel>
    
</Window>
